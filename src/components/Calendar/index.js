import React from "react";

function Calendar() {
  return (
    <div>
      <h1>
        This component will change based on the page you're on. On the BM page,
        the functionality will be to pick the day. On the Report page, it will
        let you move to reports from other days.
      </h1>
    </div>
  );
}

export default Calendar;
